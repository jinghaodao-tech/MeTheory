# Mobile workspace archived

The Expo mobile client is currently archived and is not part of the default MeTheory build, test, or dependency graph. The local-first API, analysis engine, CLI, and PCS integration are the active product surface.

The source is retained for future reactivation. Reintroducing it requires a deliberate Expo upgrade, dependency audit, and device validation before it is added back to the root workspace or CI.
