import { generateHypothesisCandidates, type CandidateGenerationConfig, type CandidateObservation, type CandidateParameter, type HypothesisCandidate } from "../../domain/src/hypothesis/candidates.ts";

export type UnderstandingRecord = { id: string; recordedAt: string; title: string; conditionValues: Record<string, unknown>; outcomeValues: Record<string, unknown> };
export type SelfUnderstandingStatus = "insufficient" | "emerging" | "stable_candidate" | "unstable" | "contradicted";
export type SelfUnderstandingConfig = Pick<CandidateGenerationConfig, "minimumSamplesPerCohort" | "minimumTotalSamples" | "maximumMissingRate" | "minimumNormalizedEffect" | "minimumSampleBalance" | "maximumCandidates"> & { stableMinimumSamples: number };
export const DEFAULT_SELF_UNDERSTANDING_CONFIG: SelfUnderstandingConfig = { minimumSamplesPerCohort: 3, minimumTotalSamples: 8, stableMinimumSamples: 12, maximumMissingRate: 0.35, minimumNormalizedEffect: 0.2, minimumSampleBalance: 0.3, maximumCandidates: 5 };
export type SelfUnderstandingInterpretationInput = { candidateId: string; period: { startAt: string; endAt: string }; condition: { fieldKey: string; label: string; groupA: string; groupB: string }; outcome: { fieldKey: string; label: string }; statistics: { groupACount: number; groupBCount: number; groupAValue: number; groupBValue: number; difference: number; missingCount: number; temporalStability: "stable" | "unstable" | "unknown" }; status: Exclude<SelfUnderstandingStatus, "insufficient">; supportingEntries: Array<{ entryId: string; recordedAt: string; title: string }>; contradictingEntries: Array<{ entryId: string; recordedAt: string; title: string }> };
export type SelfUnderstandingInterpretation = { statementJa: string; plainExplanationJa: string; supportingExplanationJa: string; contradictingExplanationJa: string; uncertaintyJa: string; nextExperiment: { title: string; durationDays: number; action: string; fieldsToRecord: string[]; successCondition: string }; selfModelCandidateJa: string };
export type SelfUnderstandingHypothesis = { id: string; construct: string; status: Exclude<SelfUnderstandingStatus, "insufficient">; statusLabelJa: string; statement: string; period: { startAt: string; endAt: string }; supportingEntryIds: string[]; contradictingEntryIds: string[]; dataShortage: string[]; confidence: number; userReview: "pending" | "fits" | "does_not_fit" | "on_hold"; nextAction: string; selfModelCandidate: string; interpretation: SelfUnderstandingInterpretation; interpretationInput: SelfUnderstandingInterpretationInput; candidate: HypothesisCandidate };

function cohortKey(value: unknown, candidate: HypothesisCandidate, parameter?: CandidateParameter) { if (String(value) === candidate.cohortA.key || String(value) === candidate.cohortB.key) return String(value); if (typeof value === "number" && parameter) { const minimum = parameter.minimumValue ?? 0; const maximum = parameter.maximumValue ?? 100; return value <= minimum + (maximum - minimum) / 2 ? "low" : "high"; } return String(value); }
function labels(candidate: HypothesisCandidate, parameters: CandidateParameter[]) { return { condition: parameters.find(item => item.id === candidate.conditionParameterId)?.nameJa ?? candidate.conditionParameterId, outcome: parameters.find(item => item.id === candidate.outcomeParameterId)?.nameJa ?? candidate.outcomeParameterId }; }
function statusFor(candidate: HypothesisCandidate, config: SelfUnderstandingConfig): Exclude<SelfUnderstandingStatus, "insufficient" | "contradicted"> { if (candidate.temporalStability === 0) return "unstable"; if (candidate.completePairCount >= config.stableMinimumSamples) return "stable_candidate"; return "emerging"; }
function statusLabel(status: SelfUnderstandingHypothesis["status"]) { return status === "stable_candidate" ? "比較的安定した候補" : status === "unstable" ? "結果が不安定" : "傾向の兆し"; }
function constructFor(candidate: HypothesisCandidate) { return `${candidate.conditionParameterId}_and_${candidate.outcomeParameterId}`.replace(/[^a-zA-Z0-9_]+/g, "_"); }
function numeric(value: number) { return Number.isInteger(value) ? String(value) : value.toFixed(2); }
function deterministicInterpretation(input: SelfUnderstandingInterpretationInput): SelfUnderstandingInterpretation { const stable = input.status === "stable_candidate"; const unstable = input.status === "unstable"; const statementJa = `「${input.condition.groupA}」の日は、「${input.outcome.label}」が${input.condition.groupB}の日より${input.statistics.difference >= 0 ? "高い" : "低い"}傾向${stable ? "が比較的安定して見られます" : "の兆しがあります"}。`; const uncertaintyJa = unstable ? "前半と後半で差の方向がそろっていないため、現在の記録では結果が不安定です。" : `ただし、記録数はまだ限られ、時間帯や予定の明確さなど別の条件が影響している可能性があります。`; const fields = [input.condition.fieldKey, input.outcome.fieldKey]; return { statementJa, plainExplanationJa: `${input.condition.groupA}の記録は${input.statistics.groupACount}件、${input.condition.groupB}の記録は${input.statistics.groupBCount}件でした。集計値はそれぞれ${numeric(input.statistics.groupAValue)}と${numeric(input.statistics.groupBValue)}で、差は${numeric(input.statistics.difference)}です。`, supportingExplanationJa: `この傾向と同じ方向の記録を${input.supportingEntries.length}件確認できます。`, contradictingExplanationJa: input.contradictingEntries.length ? `一方で、異なる方向の記録も${input.contradictingEntries.length}件あります。` : "明確な反証記録は少ないものの、反証がないことを意味しません。", uncertaintyJa, nextExperiment: { title: "小さな比較記録", durationDays: 7, action: `次の7日間、「${input.condition.label}」と「${input.outcome.label}」を同じ記録で確認します。`, fieldsToRecord: fields, successCondition: "両方の条件で少なくとも3件ずつ、同じ項目を記録する" }, selfModelCandidateJa: `私は「${input.condition.label}」の条件で「${input.outcome.label}」が変わる可能性を、今後も記録で確かめる。` }; }
function recordEvidence(record: UnderstandingRecord, candidate: HypothesisCandidate, parameter?: CandidateParameter) { const condition = record.conditionValues[candidate.conditionParameterId]; const outcome = record.outcomeValues[candidate.outcomeParameterId]; if (condition === undefined || outcome === undefined || outcome === null) return "none"; const cohort = cohortKey(condition, candidate, parameter); if (cohort !== candidate.cohortA.key && cohort !== candidate.cohortB.key) return "none"; if (typeof outcome !== "number") return cohort === candidate.cohortA.key ? "supports" : "contradicts"; const expectedHigher = candidate.relation === "a_greater_than_b"; const midpoint = (candidate.cohortA.metricValue + candidate.cohortB.metricValue) / 2; const aligns = cohort === candidate.cohortA.key ? (expectedHigher ? outcome >= midpoint : outcome <= midpoint) : (expectedHigher ? outcome < midpoint : outcome > midpoint); return aligns ? "supports" : "contradicts"; }
export function validateInterpretation(input: SelfUnderstandingInterpretationInput, output: SelfUnderstandingInterpretation): boolean { const forbidden = /(ADHD|autism|autistic|depression|bipolar|diagnos|診断|障害|疾患|必ず|間違いなく)/i; const text = [output.statementJa, output.plainExplanationJa, output.supportingExplanationJa, output.contradictingExplanationJa, output.uncertaintyJa, output.selfModelCandidateJa].join(" "); return !forbidden.test(text) && output.nextExperiment.durationDays >= 7 && output.nextExperiment.durationDays <= 14 && output.nextExperiment.fieldsToRecord.every(field => [input.condition.fieldKey, input.outcome.fieldKey].includes(field)); }
export function generateSelfUnderstanding(input: { parameters: CandidateParameter[]; observations: CandidateObservation[]; records: UnderstandingRecord[]; allowedValues?: Record<string, Array<{ valueKey: string; labelJa: string }>>; now?: string; config?: Partial<SelfUnderstandingConfig> }): SelfUnderstandingHypothesis[] { const config = { ...DEFAULT_SELF_UNDERSTANDING_CONFIG, ...input.config }; const candidates = generateHypothesisCandidates({ parameters: input.parameters, observations: input.observations, allowedValues: input.allowedValues, now: input.now, config: { ...config, lookbackDays: 30 } }); return candidates.slice(0, config.maximumCandidates).map(candidate => { const parameter = input.parameters.find(item => item.id === candidate.conditionParameterId); const supporting: string[] = []; const contradicting: string[] = []; for (const record of input.records) { const evidence = recordEvidence(record, candidate, parameter); if (evidence === "supports") supporting.push(record.id); if (evidence === "contradicts") contradicting.push(record.id); } const missing: string[] = []; if (candidate.missingConditionCount) missing.push(`条件値の欠損: ${candidate.missingConditionCount}件`); if (candidate.missingOutcomeCount) missing.push(`結果値の欠損: ${candidate.missingOutcomeCount}件`); const names = labels(candidate, input.parameters); const status = statusFor(candidate, config); const interpretationInput: SelfUnderstandingInterpretationInput = { candidateId: candidate.id, period: candidate.supportingPeriod, condition: { fieldKey: candidate.conditionParameterId, label: names.condition, groupA: candidate.cohortA.labelJa, groupB: candidate.cohortB.labelJa }, outcome: { fieldKey: candidate.outcomeParameterId, label: names.outcome }, statistics: { groupACount: candidate.cohortA.validSampleCount, groupBCount: candidate.cohortB.validSampleCount, groupAValue: candidate.cohortA.metricValue, groupBValue: candidate.cohortB.metricValue, difference: candidate.effectValue, missingCount: candidate.missingConditionCount + candidate.missingOutcomeCount, temporalStability: candidate.temporalStability === 1 ? "stable" : candidate.temporalStability === 0 ? "unstable" : "unknown" }, status, supportingEntries: input.records.filter(record => supporting.includes(record.id)).map(record => ({ entryId: record.id, recordedAt: record.recordedAt, title: record.title })), contradictingEntries: input.records.filter(record => contradicting.includes(record.id)).map(record => ({ entryId: record.id, recordedAt: record.recordedAt, title: record.title })) }; const interpretation = deterministicInterpretation(interpretationInput); return { id: candidate.id, construct: constructFor(candidate), status, statusLabelJa: statusLabel(status), statement: interpretation.statementJa, period: candidate.supportingPeriod, supportingEntryIds: supporting, contradictingEntryIds: contradicting, dataShortage: missing, confidence: Math.round(candidate.candidateScore * 100) / 100, userReview: "pending", nextAction: interpretation.nextExperiment.action, selfModelCandidate: interpretation.selfModelCandidateJa, interpretation, interpretationInput, candidate }; }); }
