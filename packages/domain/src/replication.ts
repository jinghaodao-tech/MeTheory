export type ReplicationPeriod = { period: 'discovery' | 'validation' | 'replication' | string; effectValue: number | null; normalizedEffect: number | null; direction: 'supports' | 'contradicts' | 'inconclusive' | 'insufficient_data' };
export type HypothesisPeriodSet = { discovery_start_at: string; discovery_end_at: string; validation_start_at: string; validation_end_at: string; replication_start_at: string; replication_end_at: string };
export type ReplicationStatus = 'not_started' | 'insufficient_data' | 'replicated' | 'not_replicated' | 'mixed';
export function assessReplication(periods: ReplicationPeriod[]) { const usable = periods.filter((period) => period.period !== 'discovery' && period.direction !== 'insufficient_data'); if (!usable.length) return { status: 'insufficient_data' as const, stability: null, periods }; const supporting = usable.filter((period) => period.direction === 'supports').length; const contradicting = usable.filter((period) => period.direction === 'contradicts').length; const stability = Math.max(supporting, contradicting) / usable.length; const status = supporting === usable.length ? 'supports' as const : contradicting === usable.length ? 'contradicts' as const : 'inconclusive' as const; return { status, stability, periods }; }
export function assessReplicationLifecycle(periods: ReplicationPeriod[]): { status: ReplicationStatus; stability: number | null; validation: ReplicationPeriod | null; replication: ReplicationPeriod | null } { const validation = periods.find((period) => period.period === 'validation') ?? null; const replication = periods.find((period) => period.period === 'replication') ?? null; if (!validation && !replication) return { status: 'not_started', stability: null, validation, replication }; if (!validation || !replication || validation.direction === 'insufficient_data' || replication.direction === 'insufficient_data') return { status: 'insufficient_data', stability: null, validation, replication }; if (validation.direction === replication.direction && validation.direction === 'supports') return { status: 'replicated', stability: 1, validation, replication }; if (validation.direction === replication.direction && validation.direction === 'contradicts') return { status: 'not_replicated', stability: 1, validation, replication }; return { status: 'mixed', stability: 0, validation, replication }; }
