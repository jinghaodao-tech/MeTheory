export type SyntheticObservation = { episodeId: string; parameterId: string; value: unknown; isMissing: boolean; observedAt: string };
export type SyntheticDataset = { observations: SyntheticObservation[]; conditionParameterId: string; outcomeParameterId: string; expectedDirection: 'a_greater_than_b' | 'a_less_than_b'; seed: number };
export function generateSyntheticDataset(input: { count?: number; seed?: number; startAt?: string; effect?: number }): SyntheticDataset { const count = input.count ?? 100; const seed = input.seed ?? 42; const effect = input.effect ?? 1.5; let state = seed >>> 0; const random = () => { state = (state * 1664525 + 1013904223) >>> 0; return state / 4294967296; }; const start = new Date(input.startAt ?? '2026-01-01T00:00:00.000Z'); const observations: SyntheticObservation[] = []; for (let index = 0; index < count; index += 1) { const episodeId = `synthetic_${seed}_${index}`; const high = index % 2 === 0; const noise = (random() - 0.5) * 0.6; const outcome = Math.max(1, Math.min(5, 3 + (high ? effect / 2 : -effect / 2) + noise)); const observedAt = new Date(start.getTime() + index * 86400000).toISOString(); observations.push({ episodeId, parameterId: 'condition', value: high, isMissing: false, observedAt }, { episodeId, parameterId: 'outcome', value: outcome, isMissing: index % 37 === 0, observedAt }); } return { observations, conditionParameterId: 'condition', outcomeParameterId: 'outcome', expectedDirection: 'a_greater_than_b', seed }; }
