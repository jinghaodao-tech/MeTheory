{
  "name": "metheory-review-bridge",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/index.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply review-bridge --local",
    "db:migrate:remote": "wrangler d1 migrations apply review-bridge --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
